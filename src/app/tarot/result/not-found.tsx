import ResultNotFound from "@/components/tarot/result/ResultNotFound";

/**
 * /tarot/result/* 전용 404 — 우주 배경·헤더는 result/layout에서 유지
 */
export default function TarotResultNotFoundPage() {
  return <ResultNotFound />;
}
