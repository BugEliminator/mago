import ForgetPage from "@/components/auth/forget-password/ForgetPage";

/** 비밀번호 찾기 라우트 — UI는 `components/auth/forget-password`에 둡니다. */
export default function ForgetPasswordRoutePage() {
  return <ForgetPage />;
}
